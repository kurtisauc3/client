import styled from 'styled-components';
import Colors from './Colors';

const Component = styled.div`
  height: 80px;
  border-bottom: 2px solid rgba(255, 255, 255, 0.1);
  background-color: ${Colors.DARK_PURPLE + 'EE'};
`;

export default Component;
